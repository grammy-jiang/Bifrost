# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
style = pep440
versionfile_source = bifrost/_version.py
versionfile_build =
tag_prefix =
parentdir_prefix =

[radon]
exclude = bifrost/_version.py
cc_min = B
mi_min = B